# Ralph Progress Log
## Codebase Patterns
- Next.js App Router with file-based routing in `code-front/app/`
- Dark theme colors defined in tailwind.config.ts as `dk` (dark) and `ac` (accent) palettes
- Use `next/font` for loading Noto Sans SC (variable: `--font-noto-sans-sc`)
- Glassmorphism navbar: `bg-dk-950/80 backdrop-blur-xl`
- Background effects implemented as separate `Background` component (orbs, grid, scanline)
- Page structure: `app/page.tsx` (home), `app/estimator/page.tsx`, `app/analysis/page.tsx`
- Error boundaries must be client components (`"use client"`)
- ESLint config uses `@eslint/eslintrc` FlatCompat with `eslint.config.mjs`
- Env vars need `NEXT_PUBLIC_*` prefix for browser access
- Form validation: react-hook-form + zod v4 requires `resolver: zodResolver(schema) as Resolver<FormData>` cast when using `z.coerce`
- Toast system (`use-toast` hook + `ToastContainer` component) is reusable across all pages
- API client pattern: `AbortController` + `setTimeout` for timeout handling; clear timeout in both success and error paths
- Input focus glow: `focus:border-ac-blue focus:ring-2 focus:ring-ac-blue/20`
- Chart.js components must be registered centrally via `components/chart-setup.ts` to avoid duplicate registration errors when multiple chart types are used across the app
- Use `import "@/components/chart-setup"` in any component that renders charts (Bar, Radar, etc.)

Started: 2026-05-15
---
## 2026-05-15 14:35 - US-001
- Initialized Next.js 15 project with App Router, TypeScript, Tailwind CSS in `code-front/`
- Configured dark theme (bg #030711, text #cbd5e1) with custom dk/ac color palettes in tailwind.config.ts
- Created shared layout with Noto Sans SC font, sticky navbar with glassmorphism, footer
- Implemented error.tsx (client component error boundary) and loading.tsx at layout level
- Added animated background component with floating orbs, grid pattern, scanline effect
- Created placeholder routes for /estimator and /analysis
- Set up environment variables (.env.local/.env.example) for ML_API_URL, PYTHON_API_URL, JAVA_API_URL
- Files changed: `code-front/app/*`, `code-front/components/*`, `code-front/tailwind.config.ts`, `code-front/eslint.config.mjs`, `code-front/package.json`, etc.
- **Learnings for future iterations:**
  - The project already had a partial Next.js setup (package.json, tsconfig, tailwind config) but missing app/ directory and node_modules
  - Tailwind custom colors `dk` and `ac` are already defined in the config - use these for consistency
  - Navbar active state uses `pathname === href` comparison from `usePathname()`
  - `next/font/google` loads Noto Sans SC with subset `latin` (no need for `chinese-simplified` subset which may not be available)
  - ESLint config must be `eslint.config.mjs` with FlatCompat for Next.js 15
---
## 2026-05-15 07:10 - US-002
- Updated landing page with hero section, two application cards with features/tech badges, and system architecture diagram
- Cards link to `/estimator` and `/analysis` routes with hover effects and gradient accents
- Architecture diagram shows Next.js → FastAPI/Spring Boot → ML Model flow with styled nodes
- Used existing tailwind custom colors (`dk-*`, `ac-*`) and animation classes (`animate-fade-in-up`, `animate-fade-in`)
- Responsive: grid switches to 1 column on mobile, architecture stacks vertically
- Files changed: `code-front/app/page.tsx`
- **Learnings for future iterations:**
  - `animate-fade-in` animation exists in tailwind config but needs explicit `animationFillMode: "both"` when used with inline delay styles
  - The project uses `group-hover` utilities for card interactions — no JS needed for hover states
  - Architecture diagram uses nested flex layouts; horizontal layout on desktop (`sm:flex-row`) and vertical on mobile
  - TechBadge and ArchNode are local components inside page.tsx — fine for single-page use, extract to `components/` if reused
  - ESLint and typecheck both pass cleanly, build produces static pages for all routes
---
## 2026-05-15 15:35 - US-003
- Created FastAPI project in `code-python/` with POST `/api/estimate` endpoint
- Pydantic models (`PropertyFeatures`) validate all 7 ML fields with correct ranges (square_footage 100-50000, bedrooms 0-20, etc.)
- `MLClient` class in `ml_client.py` calls upstream ML API POST `/predict` and GET `/model-info` via httpx
- `EstimateResponse` includes prediction, echoed features, model metadata (coefficients, R2, MSE, MAE), and ISO timestamp
- Error handling: ML API unavailable returns HTTP 503 via custom exception handler; invalid input returns HTTP 422 via FastAPI/Pydantic
- CORS configured for Next.js frontend origins (`http://localhost:3000`)
- 19 pytest tests covering health, estimate success, 12 validation error cases, extra field rejection, 503 ML down, model-info, CORS preflight
- mypy typecheck passes with no issues
- Files changed: `code-python/app/config.py`, `code-python/app/main.py`, `code-python/app/ml_client.py`, `code-python/app/models.py`, `code-python/tests/test_api.py`, `code-python/pyproject.toml`, `code-python/requirements.txt`, `code-python/.env.example`
- **Learnings for future iterations:**
  - `code-python` is the property estimator API (proxies to ML model); `code-api` is the actual ML model service
  - `pydantic-settings` with `SettingsConfigDict(env_file=...)` loads `.env.local` and `.env` automatically
  - Use `app.dependency_overrides[get_ml_client]` in tests to inject fake/mock ML client
  - `ConfigDict(extra="forbid")` on Pydantic models prevents unexpected fields from sneaking through
  - httpx `ASGITransport` is the modern way to test FastAPI apps asynchronously
  - Model metadata coefficients are sorted by absolute value descending (from `code-api` training)
---
## 2026-05-15 15:22 - US-004
- Created `/estimator` route with a comprehensive form containing all 7 ML fields (square_footage, bedrooms, bathrooms, year_built, lot_size, distance_to_city_center, school_rating)
- Client-side validation using React Hook Form + Zod with `@hookform/resolvers`, matching Python backend Pydantic constraints exactly
- Form styled with dark theme inputs (`bg-dk-900/60`, `border-dk-500/50`) and blue focus glow (`focus:border-ac-blue focus:ring-2 focus:ring-ac-blue/20`)
- Submit button with loading spinner state during API call, calls Python backend `POST /api/estimate`
- Error toast notifications for API/network errors using custom hook-based toast system
- Added minimal result preview panel (price + model metrics) that appears after successful prediction
- Created reusable patterns: `lib/api.ts` (API client with AbortController timeout), `hooks/use-toast.ts` (toast hook), `components/ui/toast.tsx` (toast container)
- Files changed: `code-front/app/estimator/page.tsx`, `code-front/lib/api.ts`, `code-front/hooks/use-toast.ts`, `code-front/components/ui/toast.tsx`, `code-front/package.json`, `code-front/CLAUDE.md`
- **Learnings for future iterations:**
  - With zod v4, `z.coerce.number()` infers input type as `unknown` — must cast resolver: `zodResolver(schema) as Resolver<FormData>`
  - Install `react-hook-form`, `zod`, `@hookform/resolvers` for all form-heavy stories (US-005 through US-013)
  - The toast system (`use-toast` + `ToastContainer`) is reusable across all pages — import and render at page level
  - API client pattern: `AbortController` + `setTimeout` for timeout handling; clear timeout in both success and error paths
  - Form grid uses `grid-cols-1 sm:grid-cols-2` for responsive 2-column layout on desktop
  - Input focus ring uses `focus:ring-2 focus:ring-ac-blue/20` for the blue glow effect requested in the PRD
---
## 2026-05-15 16:35 - US-006
- Created `HistoryPanel` component (`components/estimator/HistoryPanel.tsx`) displaying saved estimates from localStorage
- Each history card shows: predicted price (prominent), square footage, bedrooms, bathrooms, and timestamp
- Delete individual item button (trash icon, appears on hover) and "Clear All" button in the header
- Empty state with clock icon and "暂无历史记录" message when no entries exist
- Entry count badge displayed next to the panel title
- Page-level `historyVersion` state triggers re-read of localStorage when ResultPanel saves a new entry
- `ResultPanel` extended with optional `onSave` callback prop to notify parent of saves
- Responsive grid: 1 column on mobile, 2 on tablet, 3 on desktop
- All existing patterns followed: dark theme cards, `bg-dk-900/50`, `border-dk-500/50`, `group-hover` for delete button visibility
- Files changed: `code-front/components/estimator/HistoryPanel.tsx`, `code-front/app/estimator/page.tsx`, `code-front/components/estimator/ResultPanel.tsx`
- **Learnings for future iterations:**
  - Use a version counter pattern (`historyVersion` state) to trigger child component re-reads from localStorage without prop drilling the full data
  - `group-hover:opacity-100` + `opacity-0` is a clean pattern for action buttons that should only appear on card hover
  - `useCallback` for the `load` function in HistoryPanel prevents unnecessary effect re-runs while keeping dependencies clean
  - The `crypto.randomUUID()` IDs generated by `saveToHistory` work well as React keys
  - Maximum 50 entries with auto-purge is already handled in `lib/history.ts` — no additional logic needed in the UI
  - Manual browser verification needed (no browser automation tools configured in this environment)
---
## 2026-05-15 17:35 - US-007
- Added comparison checkboxes to each history card in `HistoryPanel`
- Selection state managed with `Set<string>`; max 3 items allowed, checkboxes disabled when limit reached
- Blue highlight border on selected cards; info hint shown when 1 item selected, success hint when 3 selected
- Created `ComparisonPanel` component with side-by-side comparison table (all 7 features + predicted price)
- Radar chart using Chart.js Radar controller with normalized feature values (0-100 scale)
- Three distinct colors (blue, cyan, purple) for chart datasets matching the table column headers
- Created shared `components/chart-setup.ts` to centralize Chart.js registration (avoids duplicate registration errors when mixing Bar and Radar charts)
- Updated `FeatureChart.tsx` to use the shared chart setup
- Files changed: `code-front/components/estimator/HistoryPanel.tsx`, `code-front/components/estimator/ComparisonPanel.tsx`, `code-front/components/estimator/FeatureChart.tsx`, `code-front/components/chart-setup.ts`
- **Learnings for future iterations:**
  - Chart.js components MUST be registered centrally to avoid "already registered" errors when multiple chart types coexist
  - `components/chart-setup.ts` is the canonical registry — import it as `import "@/components/chart-setup"` in any chart component
  - Radar chart needs: `RadarController`, `RadialLinearScale`, `PointElement`, `LineElement`, `Filler`
  - Feature normalization for radar: use the same min/max ranges as the Pydantic validators for consistency
  - Color cycling pattern: `colors[i % colors.length]` works well for 2-3 items with a predefined palette
  - Selection hints with conditional rendering provide good UX without extra UI complexity
  - Manual browser verification needed (no browser automation tools configured in this environment)
---
## 2026-05-15 18:00 - US-009
- Created `/analysis` route with four KPI cards (average price, median price, count, price per sqft)
- Each KPI card has a distinct gradient top border (blue, purple, green, amber) and icon
- Filter bar with bedrooms dropdown, year range inputs, area range inputs
- "Apply" button triggers data fetch and filters; "Reset" button clears all filters
- Filter state is synced to URL search params (bedrooms, yearMin, yearMax, areaMin, areaMax)
- Loading skeleton animation for KPI cards while data loads
- Added `fetchMarketData` and `computeMarketStats` to `lib/api.ts` with Java backend types
- Client-side filtering: filter logic runs on the fetched `MarketProperty[]` array
- Error toast notifications on fetch failure
- Files changed: `code-front/app/analysis/page.tsx`, `code-front/components/analysis/KpiCards.tsx`, `code-front/components/analysis/FilterBar.tsx`, `code-front/lib/api.ts`
- **Learnings for future iterations:**
  - The analysis page uses client-side filtering on a dataset of 100 mock properties from the Java backend
  - KPI cards use `animate-fade-in-up` with `animationFillMode: "both"` for smooth entrance
  - Filter state lives in two places: `filters` (input state) and `appliedFilters` (committed state) to avoid re-filtering on every keystroke
  - URL search params are updated via `window.history.replaceState` for client-side-only routing (no Next.js router needed since this is a client page)
  - `computeMarketStats` is a pure function that computes average, median, count, and price/sqft from `MarketProperty[]`
  - Manual browser verification needed (no browser automation tools configured in this environment)
---

## 2026-05-15 17:47 - US-008
- Created Spring Boot 3.4.4 project in `code-java/` with Java 21
- Implemented 4 REST endpoints under `/api/market`:
  - `GET /data`: generates 100 mock properties with randomized features, calls ML batch prediction via `MlClientService`
  - `GET /stats`: computes average price, median price, property count, and price per sqft
  - `GET /model-info`: fetches and caches ML model metadata (coefficients, R2, MSE, MAE) using Caffeine with 5-minute TTL
  - `GET /whatif`: runs batch predictions across a parameter range with configurable steps (2-100, default 20)
- CORS configured via both `@CrossOrigin` annotation and `WebMvcConfigurer` for Next.js frontend (`http://localhost:3000`)
- Global exception handler returning `ProblemDetail` for ML API errors (503), invalid arguments (400), and generic errors (500)
- 11 tests pass: context load, controller endpoint tests (6), service unit tests (4)
- Files changed: `code-java/pom.xml`, `code-java/src/main/java/com/property/portal/**/*.java`, `code-java/src/main/resources/application.yml`, `code-java/src/test/java/com/property/portal/**/*.java`, `code-java/CLAUDE.md`
- **Learnings for future iterations:**
  - Spring Boot 3.4.4 uses `RestClient` (introduced in 3.2) instead of `RestTemplate` for HTTP calls
  - `@Cacheable(value = "modelInfo")` + Caffeine works with `spring-boot-starter-cache` + explicit `caffeine` dependency
  - `@MockitoBean` (Spring Boot 3.4) replaces `@MockBean` for test mocking
  - `ProblemDetail` from RFC 7807 is built into Spring 6 / Boot 3 - use `application/problem+json` content type
  - Java 21 `RandomGeneratorFactory.of("L64X256MixRandom")` provides modern random number generation
  - The stats endpoint accepts an optional `properties` list; when null it auto-generates fresh data
  - Mock data generation uses the same 7 feature ranges as the ML model validators for consistency
  - What-if analysis uses reasonable defaults for unvaried fields (2000 sqft, 3 bed, 2 bath, 2010, 5000 lot, 5mi, 7 rating)
  - `BatchPredictionRequest` wraps a list of `PropertyInput` records to match the ML API's batch prediction format
---

## 2026-05-15 19:00 - US-010
- Created `MarketCharts` component (`components/analysis/MarketCharts.tsx`) with three interactive charts
- Price distribution bar chart: 6 buckets dynamically computed from min/max price, showing property count per bucket
- Average price by bedroom count bar chart: groups properties by bedroom count, computes average price per group
- Price vs area scatter plot: scatter chart using Chart.js ScatterController with squareFootage on X and predictedPrice on Y
- All charts use dark theme with slate text colors (#64748b ticks, #94a3b8 labels, rgba(28,40,68,0.4) grid lines)
- Charts receive `filtered` properties from parent, so they auto-update when filters change
- Responsive 2x2 grid: price distribution + avg by bedrooms side-by-side on desktop, scatter plot spans full width below
- Mobile: all charts stack vertically (grid-cols-1)
- Registered `ScatterController` in `components/chart-setup.ts` to enable scatter charts
- Replaced placeholder in `app/analysis/page.tsx` with `<MarketCharts properties={filtered} />`
- Files changed: `code-front/components/analysis/MarketCharts.tsx`, `code-front/components/chart-setup.ts`, `code-front/app/analysis/page.tsx`
- **Learnings for future iterations:**
  - `ScatterController` must be registered in the central chart-setup.ts alongside other Chart.js controllers
  - Scatter chart data format: `{ x: number, y: number }[]` — not `{ labels, datasets }` like bar charts
  - Scatter chart tooltip `raw` property contains the `{ x, y }` point object directly
  - For responsive multi-chart layouts, `lg:col-span-2` on the third chart creates a nice 2+1 layout in a 2-column grid
  - Common chart options can be extracted as a const and spread, but TypeScript may need `as const` assertion for deep spreads to work cleanly
  - Chart.js tooltip callbacks receive different context shapes depending on chart type (Bar vs Scatter)
  - Manual browser verification needed (no browser automation tools configured in this environment)
---
